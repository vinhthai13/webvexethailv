doctype html
html(lang="vi")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title #{title ? title + ' - ' : ''}Admin Panel
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css")
    link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css")
    link(rel="preconnect" href="https://fonts.googleapis.com")
    link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin)
    link(rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap")
    link(rel="stylesheet" href="/css/admin.css")
    block head

  body.d-flex.flex-column.min-vh-100
    header.admin-header.navbar.navbar-dark.bg-dark.shadow-sm.px-3.py-2.sticky-top
      a.navbar-brand.d-flex.align-items-center(href='/admin')
        i.fas.fa-bus.me-2
        | Đặt vé xe Admin
      
      ul.nav.nav-pills.flex-grow-1.mx-3.d-none.d-md-flex
        li.nav-item
          a.nav-link.text-white(href='/admin' class=currentPath === '/admin' ? 'active' : '')
            i.fas.fa-chart-line.me-2
            | Tổng quan
        li.nav-item
          a.nav-link.text-white(href='/admin/bookings' class=currentPath.startsWith('/admin/bookings') ? 'active' : '')
            i.fas.fa-ticket-alt.me-2
            | Đặt vé
        li.nav-item
          a.nav-link.text-white(href='/admin/schedules' class=(currentPath.startsWith('/admin/schedules') ? 'active' : ''))
            i.fas.fa-calendar-alt.me-2
            | Lịch trình
        li.nav-item
          a.nav-link.text-white(href='/admin/routes' class=(currentPath.startsWith('/admin/routes') ? 'active' : ''))
            i.fas.fa-route.me-2
            | Tuyến xe
        li.nav-item
          a.nav-link.text-white(href='/admin/users' class=(currentPath.startsWith('/admin/users') ? 'active' : ''))
            i.fas.fa-users.me-2
            | Người dùng
        li.nav-item
          a.nav-link.text-white(href='/admin/banners' class=(currentPath.startsWith('/admin/banners') ? 'active' : ''))
            i.fas.fa-images.me-2
            | Banner
         
      .dropdown
        button.btn.btn-dark.dropdown-toggle.d-flex.align-items-center(type='button' data-bs-toggle='dropdown' aria-expanded='false')
          i.fas.fa-user-circle.me-2
          | admin
        ul.dropdown-menu.dropdown-menu-end.shadow-sm.border-0
          li
            a.dropdown-item.d-flex.align-items-center(href='/admin/profile')
              i.fas.fa-user-circle.me-2.text-primary
              | Thông tin cá nhân
          li
            hr.dropdown-divider
          li
            a.dropdown-item.d-flex.align-items-center(href='/logout')
              i.fas.fa-sign-out-alt.me-2.text-danger
              | Đăng xuất

    .container-fluid.flex-grow-1.py-4
      block content

    footer.bg-dark.text-white.py-3.text-center
      p.mb-0 &copy; 2025 Đặt vé xe Admin Panel

    script(src='https://code.jquery.com/jquery-3.7.1.min.js')
    script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js')
    block scripts 